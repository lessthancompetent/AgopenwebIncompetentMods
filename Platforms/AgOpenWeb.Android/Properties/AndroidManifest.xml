<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
  <!-- All-in-one launcher: a foreground service keeps the in-process guidance host (control
       loop + UDP + LAN web feed) alive while the WebView Activity is backgrounded. specialUse
       has no daily cap (unlike dataSync on Android 15) and needs no companion-device prereq —
       right for a sideloaded in-cab host. POST_NOTIFICATIONS (API 33+) shows its notification. -->
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  <application>
    <service
        android:name="com.agopenweb.android.BackendService"
        android:exported="false"
        android:foregroundServiceType="specialUse">
      <property
          android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
          android:value="agopenweb_guidance_host" />
    </service>
  </application>
</manifest>
